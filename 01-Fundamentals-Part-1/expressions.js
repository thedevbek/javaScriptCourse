//? These are expressions that produce a value

console.log(3 + 4);
console.log(1972);
console.log(true && false || !false);

//! These are statements that do not produce a value

if (68 > 42) {
    const str = '68 is bigger than 42 and my heart';
}

console.log(`I'm ${2022 - 1972} years old.`)//you can use expressions here
//! But you can not use statements like the if statement