// # Template Literals


const job = 'Software Engineer';

const year = 2037;

const bek = "I'm " + firstName + ", a " + (year - birthYear) + " year old " + job + "!";
console.log(bek);

const bekNew = `I'm ${firstName}, a ${year-birthYear} year old ${job}!`;
console.log(bekNew);

// # use backtick instead of "" and you can press return to get to a new line!!!

console.log(`String
multiple
lines
here`);

description = `${country} is in ${continent}, and its ${population} million people speak ${language}`;
console.log(description);
